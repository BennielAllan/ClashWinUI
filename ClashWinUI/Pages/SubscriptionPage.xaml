<Page
    x:Class="ClashWinUI.Pages.SubscriptionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ClashWinUI.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page title -->
        <TextBlock
            x:Name="PageHeader"
            Grid.Row="0"
            MaxWidth="1064"
            Margin="36,24,36,0"
            AutomationProperties.HeadingLevel="Level1"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="{x:Bind PageTitle, Mode=OneWay}" />

        <!-- Command bar -->
        <CommandBar
            x:Name="CommandBarPanel"
            Grid.Row="1"
            Margin="24,4,36,0"
            HorizontalAlignment="Right"
            DefaultLabelPosition="Right">
            <AppBarButton
                x:Name="NewButton"
                Label="{x:Bind Subscription_New, Mode=OneWay}"
                Click="NewButton_Click">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE710;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <!-- Content area -->
        <Grid Grid.Row="2">

            <!-- Card list -->
            <ListView
                x:Name="SubscriptionListView"
                ItemsSource="{x:Bind SubscriptionItems, Mode=OneWay}"
                Padding="36,8,36,24"
                SelectionMode="Single"
                SelectionChanged="SubscriptionListView_SelectionChanged"
                ItemContainerTransitions="{x:Null}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Spacing="2" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:SubscriptionItem">
                        <Border Padding="16,14">
                            <Grid RowSpacing="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Top row: name + url | actions -->
                                <Grid Grid.Row="0" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="2">
                                        <TextBlock
                                            Text="{x:Bind Name, Mode=OneWay}"
                                            Style="{StaticResource BodyStrongTextBlockStyle}"
                                            TextTrimming="CharacterEllipsis" />
                                        <TextBlock
                                            Text="{x:Bind UrlOrPath, Mode=OneWay}"
                                            FontSize="11"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            TextTrimming="CharacterEllipsis"
                                            Visibility="{x:Bind HasUrlOrPathVisibility, Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                                        <!-- Refresh button / progress ring -->
                                        <Grid Width="32" Height="32">
                                            <ProgressRing
                                                IsActive="{x:Bind IsRefreshing, Mode=OneWay}"
                                                Visibility="{x:Bind RefreshRingVisibility, Mode=OneWay}"
                                                Width="16"
                                                Height="16"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center" />
                                            <Button
                                                Tag="{x:Bind}"
                                                Style="{StaticResource TransparentIconButtonStyle}"
                                                Visibility="{x:Bind RefreshButtonVisibility, Mode=OneWay}"
                                                ToolTipService.ToolTip="{x:Bind RefreshTooltip, Mode=OneTime}"
                                                Click="SubscriptionRefresh_Click"
                                                Width="32"
                                                Height="32">
                                                <FontIcon
                                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                    Glyph="&#xE72C;"
                                                    FontSize="13" />
                                            </Button>
                                        </Grid>

                                        <!-- More button -->
                                        <Button
                                            Tag="{x:Bind}"
                                            Style="{StaticResource TransparentIconButtonStyle}"
                                            ToolTipService.ToolTip="{x:Bind MoreTooltip, Mode=OneTime}"
                                            Click="SubscriptionMore_Click"
                                            Width="32"
                                            Height="32">
                                            <FontIcon
                                                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                Glyph="&#xE712;"
                                                FontSize="13" />
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <!-- Bottom row: usage bar + timestamps -->
                                <Grid Grid.Row="1" ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Spacing="4">
                                        <ProgressBar
                                            Maximum="1"
                                            Value="{x:Bind UsageProgress, Mode=OneWay}"
                                            Height="4"
                                            CornerRadius="2" />
                                        <TextBlock
                                            Text="{x:Bind UsageTotalDisplay, Mode=OneWay}"
                                            FontSize="11"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>

                                    <TextBlock
                                        Grid.Column="1"
                                        Text="{x:Bind RefreshedAgoDisplay, Mode=OneWay}"
                                        FontSize="11"
                                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                        VerticalAlignment="Bottom" />
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Empty state -->
            <StackPanel
                x:Name="EmptyStatePanel"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16"
                Visibility="Collapsed">
                <FontIcon
                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                    Glyph="&#xE8F1;"
                    FontSize="52"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                <StackPanel HorizontalAlignment="Center" Spacing="4">
                    <TextBlock
                        Text="{x:Bind Subscription_EmptyTitle, Mode=OneWay}"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                    <Button
                        Content="{x:Bind Subscription_New, Mode=OneWay}"
                        Click="NewButton_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
